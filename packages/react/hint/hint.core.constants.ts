const DEFAULT_HINT_POSITIONING = {
  align: 'center',
  arrowPadding: 4,
  closeDelay: 100,
  flip: true,
  gutter: 10,
  openDelay: 0,
  overflowPadding: 8,
  placement: 'top'
} as const

export { DEFAULT_HINT_POSITIONING }
