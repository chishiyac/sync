import { tv } from 'tailwind-variants'

const noiseTextureRootRecipe = tv({
  base: 'pointer-events-none absolute inset-0 z-0 size-full opacity-50 select-none dark:opacity-[0.75]'
})

export { noiseTextureRootRecipe }
