import { tv } from 'tailwind-variants'

const removeAccountContentRecipe = tv({
  base: 'z-[500] w-full max-w-sm!'
})

const removeAccountPositionerRecipe = tv({
  base: 'z-[500]'
})

export { removeAccountContentRecipe, removeAccountPositionerRecipe }
